# Write a script which accepts decimal number from user and converts it to hexadecimal.

decimal_input = int(input("Give me some decimal numbers i will work with"))
hexed_number = hex(decimal_input)
print(hexed_number)
